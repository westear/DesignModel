责任链模式: 使多个对象都有机会处理请求，从而避免了请求的发送者和接受者之间的耦合关系。将这些对象连成一条链，并沿着这条链传递该请求，直到对象处理它为止。

demo1 模拟 古代妇女三从四德的场景： 未嫁从父，既嫁从夫，夫死从子， 决定权符合责任链模式
demo2 是责任链模式的通用代码模板

优点：
    将请求和处理分开，请求者并不知道具体哪个角色或节点处理了请求

缺点：
    如果调用链过长，节点较多时，容易造成性能问题