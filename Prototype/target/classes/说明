原型模式: 用原型实例指定创建对象的种类，并且通过拷贝这些原型创建的对象。

优点:
    性能优良，比 new 一个对象好很多
    逃避构造器的约束，直接从内存中拷贝，构造函数不会执行

使用场景:
    资源优化
    性能安全（线程安全）
    一个对象，多个修改者

注意事项：
    构造函数不会被执行: Object类的clone方法是从内存中（堆内存）以二进制流的方式进行拷贝，重新分配一块内存块，不执行构造函数。
    深拷贝和浅拷贝:
        浅拷贝测试示例为demo2；
        深拷贝测试示例为demo3