意图： 使控制范围之外的一个原因对象与某个接口匹配
问题： 系统的数据和行为都正确，但接口不服。通常用于必须从抽象类派生时。
解决方案： Adapter 模式提供了具有所需接口的包装类
效果： Adapter 模式使用原有对象能够适应新的类结构，不受其接口限制。
实现: 将原有类包含和另一个类之中。让包含类与需要的接口匹配，调用被包容类的方法。（第二种方法可以通过多继承实现）

优点： 
    1.让两个没有关系的类一起运行
    2.增加了类的透明性
    3.提高了类的复用度
    4.灵活性非常好